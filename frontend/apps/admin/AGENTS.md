# apps/admin — 管理端

> 职责：教师/教务/管理员/校长端（core §2 四类管理向角色的入口应用）。
> 规则：业务页面随所属域任务包落位（任务包默认完整纵切）；跨应用公共代码提 `packages/shared`；页面调用后端一律走 `src/api/http.ts` 实例（契约解包/401 行为统一）。
> 维护者：协调者侧（骨架）——交付署名 agent-fffabc；各页面归属见对应域任务包
