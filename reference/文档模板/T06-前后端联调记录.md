# {{项目名称}} 前后端联调记录

> 适用：P06 成果③（登录页 + 列表页 + 详情页端到端联调）、P13 成果①（所有页面调用后端 API 的演示证据）。
> 这份文档记录"前端和后端真的对上了"，以及中间踩过的坑。
>
> **考核依据**（《软件项目开发综合实验》教学大纲六·过程性考核·阶段性成果展示，占 20%）：
> 本模板同时支撑 P06 与 P13 两次阶段性成果展示，归档时是前后端集成工作的主要证据。
> 直接对应 **课程目标 4**（前后端融合 / 跨域协调 / 复杂企业级方案）的考核。
>
> 直接复制本模板，把 `{{...}}` 替换成你的内容， **删除所有斜体提示文字后再提交**。

---

## 0. 提交信息

| 项          | 内容                             |
|-------------|----------------------------------|
| 课程        | 软件项目开发综合实验             |
| 班级 / 组号 | {{如：2023级软工1班 · G05}}      |
| 项目名称    | {{}}                             |
| 对应课次    | {{P06 / P13}}                    |
| 成员与分工  | {{姓名（学号）— 角色，逐行列出}} |
| 提交日期    | {{YYYY-MM-DD}}                   |
| 版本        | v1.0                             |

---

## 1. 联调环境

| 项       | 说明                              |
|----------|-----------------------------------|
| 前端地址 | {{http://localhost:5173}}         |
| 后端地址 | {{http://localhost:8080}}         |
| 代理配置 | {{vite.config.ts 中 /api → 8080}} |
| 数据库   | {{MySQL 8.0，库名 bookshop}}      |
| 联调日期 | {{YYYY-MM-DD}}                    |

---

## 2. 跨域方案

> *说清是怎么解决 CORS 的：后端配置还是前端代理，二选一并说明原因。*

- 采用方案：{{后端 CorsConfig / 前端 Vite 代理}}
- 关键配置：

```java
// {{CorsConfig.java}}
registry.addMapping("/**")
        .

allowedOriginPatterns("{{http://localhost:5173}}")
        .

allowedMethods("GET","POST","PUT","DELETE","OPTIONS")
        .

allowCredentials(true);
```

- 遇到的跨域问题及解决：{{如：携带凭证时不能用 allowedOrigins ("*")，改为 allowedOriginPatterns}}

---

## 3. Token 注入方式

> *登录后 token 怎么存、怎么带到每个请求上。*

- 存储位置：{{localStorage / Pinia}}
- 注入方式：{{axios 请求拦截器读取并写入 Authorization 头}}
- 关键代码：

```ts
// {{src/utils/request.ts}}
service.interceptors.request.use(config => {
    const token = localStorage.getItem('token')
    if (token) config.headers.Authorization = `Bearer ${token}`
    return config
})
```

- 401 处理：{{响应拦截器统一跳转登录页}}

---

## 4. 联调清单

| # | 页面           | 调用接口                 | 方法 | 联调结果 | 遇到的问题               | 解决方式                      |
|---|----------------|--------------------------|------|----------|--------------------------|-------------------------------|
| 1 | {{登录页}}     | {{/api/v1/users/login}}  | POST | ✅ 通过  | {{无}}                   | {{—}}                         |
| 2 | {{图书列表页}} | {{/api/v1/books?page=1}} | GET  | ✅ 通过  | {{分页参数未传，返回空}} | {{前端补 page/size 默认参数}} |
| 3 | {{图书详情页}} | {{/api/v1/books/{id}}}   | GET  | ✅ 通过  |                          |                               |
| 4 |                |                          |      |          |                          |                               |
| 5 |                |                          |      |          |                          |                               |
| 6 |                |                          |      |          |                          |                               |

> P06 至少覆盖登录页、列表页、详情页三个页面；P13 需覆盖全部页面的 CRUD 全流程。

---

## 5. 联调证据

| # | 内容             | 文件路径                          | 说明                         |
|---|------------------|-----------------------------------|------------------------------|
| 1 | {{登录成功跳转}} | {{evidence/P06-证据-01-登录.png}} | {{}}                         |
| 2 | {{列表数据渲染}} | {{evidence/P06-证据-02-列表.png}} | {{}}                         |
| 3 | {{详情页数据}}   | {{evidence/P06-证据-03-详情.png}} | {{}}                         |
| 4 | {{演示视频}}     | {{evidence/P13-演示.mp4}}         | {{P13 需要，含 CRUD 全流程}} |

> 截图命名统一：`P{课次}-证据-{序号}-{简述}.png`，统一放 `evidence/` 目录。

---

## 6. 未联调通过项

| #    | 页面 / 接口 | 阻塞原因 | 计划解决时间 | 负责人 |
|------|-------------|----------|--------------|--------|
| {{}} | {{}}        | {{}}     | {{}}         | {{}}   |

---

## 提交前自检

- [ ] P06 三个核心页面（登录 / 列表 / 详情）全部调通
- [ ] P13 所有页面 API 调用成功，有演示视频或完整截图
- [ ] 跨域方案说清楚，附关键配置代码
- [ ] Token 注入与 401 处理已实现
- [ ] 证据文件路径真实可打开，命名符合规范
- [ ] 所有 `{{...}}` 已替换，斜体提示已删除

## 评分要点（对应 P06 成果③ / P13 成果①）

| 维度       | 权重 | 标准                                       |
|------------|------|--------------------------------------------|
| 联调完成度 | 40%  | P06 覆盖三页；P13 覆盖全部页面 CRUD 全流程 |
| 方案正确   | 25%  | 跨域与 Token 方案合理，配置代码可复用      |
| 证据充分   | 25%  | 截图 / 视频清晰可辨，能看出真实数据        |
| 问题记录   | 10%  | 遇到的坑记录完整，未通过项有明确计划       |
