# 域/模块: 平台底座/启动器
# 类型: 本地开发配置模板（不入库）
# 职责: 提供本机 MySQL 凭证。使用方法：把本文件复制为同目录 application-local.yml 并填入真实值；
#       application-local.yml 已被 .gitignore 忽略，凭证永远不会进仓库。
#       等价替代：不改文件，直接设环境变量 SLATE_MYSQL_USER / SLATE_MYSQL_PASSWORD。
#       数据库 slate 不需要手工创建（连接串含 createDatabaseIfNotExist，启动时自动建库建表+种子）。
# 设计文档: docs/design/平台底座/design.md
# 维护者: 协调者 / agent-fffabc
spring:
  datasource:
    username: your_mysql_username
    password: your_mysql_password
